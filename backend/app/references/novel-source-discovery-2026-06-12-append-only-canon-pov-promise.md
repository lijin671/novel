# Novel source discovery: append-only canon POV promise ledger pulse (2026-06-12)

Boundary: `public_git_lsremote_raw_readme_license_no_login_no_clone_no_runtime`.

This intake used public `git ls-remote`, raw README/LICENSE markers, and compact search metadata only. It did not clone, install, run a Codex skill, call providers, read local project manuscripts, import upstream prompts, or execute scripts.

## Source reviewed

- `jiaw-Zh/long-novel-writer` ? observed HEAD `ec1604e414bfafece8142f469b05a960d21b3a9a`; no license file observed in static raw marker pass.

Public README markers describe:

- append-only Canon Ledger files
- `canon/facts.jsonl` with source references and `known_by` visibility
- `canon/promises.jsonl` for vows/obligations with due dates
- `canon/progression.jsonl` for monotonic ability/progression checks
- `canon/rules.md` and `canon/timeline.md`
- write-before context assembly and write-after validation
- POV knowledge boundary checks
- hard-conflict blocking for numeric drift, ability overreach, missed promises, and resurrection conflicts

## Pattern absorbed

`append_only_canon_pov_promise_gate`

Durable rule:

- Continuation should assemble only relevant ledger slices, not the whole book.
- Facts, promises, progression, rules, and timeline stay append-only by default.
- POV scenes must filter facts through `known_by` or an equivalent visibility matrix.
- Accepted chapters must validate promise deadlines, progression monotonicity, numeric drift, ability overreach, and impossible resurrection/state conflicts before canon promotion.

## Deferred/runtime gates

Runtime remains blocked for Codex skill activation, prompt import, project file import, generated fiction import, provider/model calls, shell scripts, package managers, and any local manuscript indexing. The source is pattern-only until license/provenance and runtime safety are separately reviewed.

## Integration surface

The project now has default discovery query/URL coverage, static repository override notes, pattern-keyword detection, pattern-pack hints, bible enrichment targets, whole-book analysis targets, continuation state/prompt hints, same-type transformation/copy-risk guidance, frontend type/rendering, digest rendering, and regression tests for this gate.
