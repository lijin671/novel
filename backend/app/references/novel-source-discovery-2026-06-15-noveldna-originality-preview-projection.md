# NovelDNA originality preview projection

Date: 2026-06-15

## Source posture

- `hayrgpt-rgb/NovelForge-AI` remains pattern-only.
- `D:/project/universal-novel-writing` remains local static reference.
- No upstream runtime, service, Docker stack, provider call, prompt body, agent
  file, generated prose, source chunk, cookie, token, or credential was imported.

## Absorbed pattern

The continuation preview now projects two NovelForge-derived gates into the
project-native remix context:

1. `source_novel_dna_fusion_boundary_gate`
   - keep `SourceNovelChunk` and source scene summaries in the source-analysis
     layer only
   - allow fusion design to read abstract `SourceNovelAnalysis`, `NovelDNA`,
     `FusionBlueprint`, taboo elements, and risk metadata
   - block promotion when raw markers such as `raw_excerpt`, `source_text`,
     `verbatim_excerpt`, or `source_novel_raw` appear in writing context

2. `originality_guard_project_creation_gate`
   - require an explicit user-triggered originality check before a fusion
     result creates or continues a formal project
   - rerun the guard at project-creation time against the current fusion result
   - treat unresolved rights, high/critical risk, forbidden similarities, stale
     checks, and raw-source markers as blockers

## Project projection

Continuation context now exposes:

- control axes for source-analysis to NovelDNA boundary, writing-layer chunk
  exclusion, raw-source marker scans, explicit originality checks, create-project
  rechecks, and high-risk blockers
- warning buckets:
  - `source_novel_dna_fusion_warnings`
  - `originality_guard_warnings`
- same-type independence checks that transfer only abstract NovelDNA and
  FusionBlueprint shape while requiring new premise, cast, world rules,
  forbidden-similarity terms, rights evidence namespace, and originality check id

## Runtime boundary

This is static projection only. It does not run plagiarism services, MCP servers,
external originality APIs, provider calls, GitHub clones, upstream tests, Docker,
or source manuscript uploads.
