# Novel source discovery: longform local skill workbench pulse (2026-06-12)

Boundary: `public_git_lsremote_raw_readme_license_root_marker_no_login_no_clone_no_runtime`.

This intake reviewed public repository metadata, public `git ls-remote` HEADs, raw README/LICENSE markers, and root-marker hints only. It did not clone, install, run scripts, start servers, launch browsers, read browser storage, call providers, start MCP tools, upload manuscripts, or import external source text.

## Sources reviewed

- `oaidea/novel-studio` ? HEAD `0a9eaddcba51ef47421acaadb2ca74dccc9cd03d`, MIT. Absorbed as `packet_first_style_overlay_context_gate` from packet-first chapter writing, style overlay, object-state summary, project-level style modeling, serialization management, and low-token context orchestration.
- `DuckTraDo/Novel` ? already tracked default source. Absorbed additional `lora_style_adapter_memory_bank_gate` from `style_bank.jsonl`, local inference, memory/story_bible files, chapter reset boundaries, and LoRA adapter posture.
- `aszecsei/writr` ? HEAD `81109d9af46e6f0cf02a7ce6f110b5b7b6116bd5`, MIT. Absorbed as `browser_local_story_bible_privacy_gate` from local-first browser storage, no accounts/cloud sync/tracking, Story Bible context, timeline, style guide, worldbuilding docs, autosave, and export markers.
- `vivy1024/novelfork` ? HEAD `0e9b30775dc0a386233a1b963ea676c1941108e9`, MIT. Absorbed as `jingwei_layered_canon_plugin_gate` from Canon/Dynamic/Reference layering, scene.spec, pipeline.write, audit+revise, session recovery, plugin UI, SQLite, and MCP extension markers.
- `felixchaos/rpg-roleplay-platform` ? HEAD `e3c5e2d9e1c010a385c0ecfe5d49e9ecf9b39fec`, AGPL-marked license preview. Absorbed as `roleplay_branchable_save_world_gate` from novel-to-playable-world, branching saves, worldbook/canon repo, script ingestion, character cards, timeline, token accounting, MCP/tool DSL, Docker/setup/provider surfaces.

## Durable patterns added

- Packet-first continuation gate: build chapter packets from recap, style overlay, object-state summary, serialization state, omitted-context notes, and token budget before drafting.
- Style-bank/adapter gate: keep style memory as permission-safe abstract levers; runtime adapters and local inference require a separate safety contract.
- Browser-local privacy gate: distinguish browser-local, exported, user-selected, and public Story Bible context before provider payload construction.
- Jingwei layered canon gate: separate Canon, Dynamic, and Reference layers; scene.spec proposes, pipeline.write drafts, audit+revise reports, and only approved deltas promote to Canon.
- Branchable save/world gate: keep original text retrieval, worldbook facts, branch saves, sandbox events, and accepted novel canon in separate ledgers.

## Deferred/runtime gates

Runtime remains blocked for Bun/Node installs, Python setup scripts, Docker stacks, browser local storage reads, provider calls, MCP tools, SQLite databases, vector indexes, uploaded manuscripts, LoRA/model files, and RPG/playable-world servers until an explicit local safety contract exists.

## Verification targets

The code integration adds default GitHub queries/URLs, static repository overrides, pattern keywords, pattern-pack hint fields, bible and whole-book targets, continuation hints, same-type remap/transformation/copy-risk hints, frontend type/panel rendering, and regression tests.
